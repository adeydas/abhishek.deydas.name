version: 0.2

phases:
  install:
    commands:
      - gem install bundler jekyll
      - gem install jekyll-last-modified-at
      - gem install minima
  build:
    commands:
      - jekyll build
artifacts:
  files:
    - '_site/**/*'
  secondary-artifacts:
  artifact1:
    files:
      - '_site/**/*'